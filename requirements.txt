# Dependencias para correr TP2_PINN_muestreo.ipynb localmente (fuera de Colab).
# En Google Colab estos paquetes ya vienen preinstalados; no hace falta instalar nada.
#
# Si querés fijar las versiones EXACTAS que se usaron en tu corrida de Colab, corré esto
# en una celda del notebook y reemplazá este archivo con la salida:
#   !pip freeze | grep -iE "torch|numpy|scipy|matplotlib|pandas"

torch>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
